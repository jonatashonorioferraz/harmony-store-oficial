"use client";
import { useEffect, useState } from "react";
import { optimizeImageUpload, VISUAL_REFERENCE_MAX_BYTES } from "../../image-upload";

const SHOTS = [
  ["catalog-cover", "1. Capa principal"], ["product-detail", "2. Detalhe e acabamento"],
  ["variations", "3. Cores e variações"], ["purchase-contents", "4. Conteúdo da compra"],
  ["use-occasion", "5. Ocasião de uso"], ["product-size", "6. Tamanho e medidas"],
] as const;
const CATEGORIES = ["Mini sabonetes", "Organza", "Caixinha personalizada", "Sagu perfumado", "Outro produto artesanal"];

export default function VisualStandardsPage() {
  const [category, setCategory] = useState(CATEGORIES[0]); const [data, setData] = useState<any>(null); const [message, setMessage] = useState(""); const [busy, setBusy] = useState("");
  const [reference, setReference] = useState({ title: "", shotType: SHOTS[0][0], guidance: "", neverDo: "Não copiar o formato do produto da referência\nNão adicionar textos não previstos\nNão alterar o produto real" }); const [image, setImage] = useState<File | null>(null);
  const load = async () => { const response = await fetch("/api/admin/overview", { cache: "no-store" }); const value = await response.json(); if (!response.ok) throw new Error(value.error); setData(value); };
  useEffect(() => { load().catch((error) => setMessage(error.message)); }, []);
  const consolidate = async (shotType: string) => { setBusy(shotType); setMessage(""); try { const response = await fetch("/api/admin/visual-standards", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "consolidate", category, shotType, reason: "Manual consolidado a partir de referências analisadas e aprovadas" }) }); const value = await response.json(); if (!response.ok) throw new Error(value.error); setMessage(`Manual v${value.version} criado como rascunho. Revise o resumo e publique quando estiver correto.`); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Falha ao consolidar"); } finally { setBusy(""); } };
  const publish = async (standard: any) => { if (!confirm("Publicar este manual para o Fotógrafo Virtual?")) return; setBusy(standard.shot_type); try { const response = await fetch("/api/admin/visual-standards", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "publish", id: standard.id, category, shotType: standard.shot_type }) }); const value = await response.json(); if (!response.ok) throw new Error(value.error); setMessage("Manual publicado e conectado ao agente."); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Falha ao publicar"); } finally { setBusy(""); } };
  const upload = async () => { if (!image || !reference.title.trim() || !reference.guidance.trim()) return; setBusy("upload"); setMessage("Otimizando e analisando a referência…"); try { const optimized = await optimizeImageUpload(image, { maxBytes: VISUAL_REFERENCE_MAX_BYTES, maxDimension: 1800 }); const form = new FormData(); form.append("image", optimized); form.append("title", reference.title); form.append("scope", "category"); form.append("category", category); form.append("modelName", ""); form.append("shotType", reference.shotType); form.append("transferMode", "style-composition"); form.append("guidance", reference.guidance); form.append("neverDo", reference.neverDo); const response = await fetch("/api/admin/visual-references", { method: "POST", body: form }); const value = await response.json(); if (!response.ok) throw new Error(value.error); const approved = confirm(`Análise automática:\n\nComposição: ${value.analysis.composition}\nIluminação: ${value.analysis.lighting}\nFundo: ${value.analysis.background}\nPaleta: ${value.analysis.palette}\n\nAprovar para o aprendizado?`); const decision = await fetch("/api/admin/visual-references", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: approved ? "approve-analysis" : "reject-analysis", id: value.id }) }); if (!decision.ok) throw new Error("Não foi possível registrar sua decisão"); setReference((current) => ({ ...current, title: "", guidance: "" })); setImage(null); setMessage(approved ? "Referência aprovada. Ela já pode participar da consolidação do manual." : "Referência mantida fora do aprendizado."); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Falha ao cadastrar referência"); } finally { setBusy(""); } };
  if (!data) return <main className="admin-loading">Carregando governança visual…</main>;
  return <main className="admin-shell"><aside><a href="/admin">← Centro de Inteligência</a><h1>Manuais<br/>Visuais</h1><p>O agente recebe apenas o manual publicado da categoria e da função atual.</p></aside><section className="admin-main"><header><div><span>GOVERNANÇA VISUAL</span><h2>Seis funções do anúncio</h2></div><b>Revisão humana obrigatória</b></header><label>Categoria<select value={category} onChange={(event) => setCategory(event.target.value)}>{CATEGORIES.map((item) => <option key={item}>{item}</option>)}</select></label>{message && <div className="admin-error">{message}</div>}<section className="agent-editor"><div className="editor-head"><div><small>NOVA REFERÊNCIA</small><h3>Ensinar e revisar um padrão</h3></div></div><div className="editor-grid"><label>Função da imagem<select value={reference.shotType} onChange={(event) => setReference({ ...reference, shotType: event.target.value })}>{SHOTS.map(([id, label]) => <option value={id} key={id}>{label}</option>)}</select></label><label>Nome da referência<input value={reference.title} onChange={(event) => setReference({ ...reference, title: event.target.value })}/></label></div><label>Imagem aprovada<input type="file" accept="image/png,image/jpeg,image/webp" onChange={(event) => setImage(event.target.files?.[0] ?? null)}/></label><label>O que deve ser transferido<textarea value={reference.guidance} onChange={(event) => setReference({ ...reference, guidance: event.target.value })}/></label><label>O que nunca copiar<textarea value={reference.neverDo} onChange={(event) => setReference({ ...reference, neverDo: event.target.value })}/></label><button className="admin-primary" disabled={busy === "upload" || !image || !reference.title.trim() || !reference.guidance.trim()} onClick={upload}>{busy === "upload" ? "Analisando…" : "Analisar referência"}</button></section><div className="admin-cards">{SHOTS.map(([id, label]) => { const references = data.visualReferences.filter((item: any) => item.category === category && item.shot_type === id && item.status === "active" && item.analysis_status === "approved"); const versions = data.visualStandards.filter((item: any) => item.category === category && item.shot_type === id); const published = versions.find((item: any) => item.status === "published"); const draft = versions.find((item: any) => item.status === "draft"); return <article key={id}><small>{label}</small><h3>{published ? `Manual v${published.version} conectado` : "Sem manual publicado"}</h3><p>{references.length} referência(s) analisada(s) e aprovada(s). Mínimo: 3.</p>{published && <div><span>✓ O Fotógrafo Virtual usa este manual</span></div>}{draft && <details><summary>Revisar rascunho v{draft.version}</summary><pre>{JSON.stringify(draft.specification, null, 2)}</pre><button onClick={() => publish(draft)}>Aprovar e publicar</button></details>}<footer><button disabled={busy === id || references.length < 3} onClick={() => consolidate(id)}>{busy === id ? "Consolidando…" : "Consolidar novo manual"}</button></footer></article>; })}</div></section></main>;
}
